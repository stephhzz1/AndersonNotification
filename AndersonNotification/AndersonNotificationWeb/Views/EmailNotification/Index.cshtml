
@{
    ViewBag.Title = "Index";
}

<div ng-controller="EmailNotificationController as model" ng-init="model.Initialise()">
    <div class="row">
        <div class="col-sm-4">
            <a href="../EmailNotification/Create" class="btn btn-default">Create</a>
        </div>
        <br />
    </div>
    <div class="row">
        <div class="col-sm-12">
            <table class="table table-hover table-sm table-bordered ">
                <thead>
                    <tr class="thead-inverse">
                        <th>Notification ID</th>
                        <th>To</th>
                        <th>From</th>
                        <th>Subject</th>
                        <th>Message</th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="clickableRow" ng-repeat="emailnotification in model.EmailNotifications" ng-click="$event.originalEvent.dropdown || model.GoToUpdatePage(emailnotification.emailNotificationId)" ng-init="model.UpdateEmailNotification(emailnotification)">
                        <td><span ng-bind="emailnotification.NotificationId"></span></td>
                        <td><span ng-bind="emailnotification.Sender"></span></td>
                        <td><span ng-bind="emailnotification.Receiver"></span></td>
                        <td><span ng-bind="emailnotification.Subject"></span></td>
                        <td><span ng-bind="emailnotification.Body"></span></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>