
@{
    ViewBag.Title = "Index";
}

<div ng-controller="NotificationController as model" ng-init="model.Initialise()">
    <div class="row">
        <div class="col-sm-4">
            <a href="../Notification/Create" class="btn btn-default">Create</a>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <table class="table table-hover table-sm table-bordered ">
                <thead>
                    <tr class="thead-inverse">
                        <th>Notification ID</th>
                        <th>To</th>
                        <th>From</th>
                        <th>Subject</th>
                        <th>Message</th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="clickableRow" ng-repeat="notification in model.Notifications" ng-click="$event.originalEvent.dropdown || model.GoToUpdatePage(notification.NotificationId)" ng-init="model.UpdateEmployee(notification)">
                        <td><span ng-bind="notification.NotificationId"></span></td>
                        <td><span ng-bind="notification.To"></span></td>
                        <td><span ng-bind="notification.From"></span></td>
                        <td><span ng-bind="notification.Subject"></span></td>
                        <td><span ng-bind="notification.Message"></span></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>